name: helper

on:
  workflow_call:
    inputs:
      file_path:
        description: test
        type: string
jobs:
  find-file-please:
    runs-on: ubuntu-latest
    steps:
      - name: List current path
        shell: bash
        run: ls -al ../
      
      - name: List current path
        shell: bash
        run: ls -al ../../
      - name: Uses file
        shell: bash
        run: |
          value=`cat ${{inputs.file_path}}`
          echo The result is $value